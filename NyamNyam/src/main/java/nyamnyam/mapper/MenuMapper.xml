<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace = "nyamnyam.mapper.MenuMapper">
<insert id = "menuInsert" parameterType = "nyamnyam.domain.MenuDTO">
insert into menu (menu_num, menu_name, menu_price, menu_content, menu_origin, menu_kind, menu_image, menu_store_image)	
values (#{menuNum}, #{menuName}, #{menuPrice}, #{menuContent}, #{menuOrigin}, #{menuKind}, #{menuImage}, #{menuStoreImage})
</insert>
</mapper>